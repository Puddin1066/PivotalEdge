-- Placeholder migration: catalyst-markets track tables (Postgres + pgvector).
-- Apply when packages/db lands; fixtures remain SoR until then.

-- CREATE EXTENSION IF NOT EXISTS vector;
-- CREATE TABLE catalyst_events (...);
