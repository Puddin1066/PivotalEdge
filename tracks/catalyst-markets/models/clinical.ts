export { estimatePSuccessBaseline } from "./baselines.js";

/** Placeholder for embedding-augmented clinical model (Model 2). */
export function estimatePSuccessEmbeddingAugmented(
  baselineP: number,
  _embeddingNorm?: number,
): number {
  return baselineP;
}
